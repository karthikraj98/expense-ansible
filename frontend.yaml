- name: frontend configuration
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package: dnf install nginx -y
    name: nginx
    state: present

  - name: nginx service
    ansible.builtin.service:
     state: restarted
     enabled: yes  